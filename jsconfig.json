{
    "compilerOptions": {
        "baseUrl": ".", // Adding baseUrl to set the root of the project
        "paths": {
            "@/*": ["./*"], // Existing path alias
            "@/lib/*": ["lib/*"] // New path alias for the lib directory
        }
    }
}
